#include<stdio.h>
main()
{
    int a,b,temp;
    printf("enter value of a and b");
    scanf("%d%d",&a,&b);
    printf("before swapping a=%d and b=%d",a,b);
    temp=a;
    a=b;
    b=temp;
    printf("\n after swapping a=%d and b=%d",a,b);
}
